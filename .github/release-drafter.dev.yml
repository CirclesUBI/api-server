template: |
  ## Changes

  $CHANGES

name-template: "`pre-v$RESOLVED_VERSION` unstable"
tag-template: "pre-v$RESOLVED_VERSION"
tag-prefix: "pre-v"

version-template: "$COMPLETE"

change-template: '- $TITLE @$AUTHOR (#$NUMBER)'
change-title-escapes: '\<*_&#@`'

include-labels:
  - 'release/dev'

version-resolver:
  major:
    labels:
      - 'major'
  minor:
    labels:
      - 'minor'
  patch:
    labels:
      - 'patch'
  default: patch
