describe("direct transfer of circles", () => {
  it("should send the receiver's own tokens first", () => {
  });
  it("then should send all other holdings except the own", () => {
  });
  it("at last is should send the own tokens", () => {
  });
});
